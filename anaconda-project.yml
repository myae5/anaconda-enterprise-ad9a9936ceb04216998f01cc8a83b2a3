name: Gapminder Visualization
description: An example Bokeh application.

commands:
  default:
    unix: bokeh serve . --address {{address}} --allow-websocket-origin {{host}} --port {{port}} --use-xheaders

packages:
  - bokeh
  - pandas
  - numpy
  - ipykernel
  
platforms:
  - linux-64

env_specs:
  anaconda202011_py38: {}
